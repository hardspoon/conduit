# Database
DATABASE_URL="postgresql://postgres:password@localhost:5432/conduit"

# Next Auth
# Generate a secret: `openssl rand -base64 32` or use `node scripts/generate-secret.js`
NEXTAUTH_SECRET="your-secret-key-at-least-32-chars-long"
NEXTAUTH_URL="http://localhost:3000"

# For production, use your Railway-provided URL
# NEXTAUTH_URL="https://your-app.up.railway.app"

# Railway-specific (these will be set automatically in production)
# DATABASE_URL="postgresql://${PGUSER}:${PGPASSWORD}@${PGHOST}:${PGPORT}/${PGDATABASE}"
